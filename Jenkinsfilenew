 def pipeline
    node('jenkins-worker') {
        dir('mavenpipeline')
        {
        git branch: 'master', url: 'https://github.com/kumartdp/mavenpipeline.git'
         }
        pipeline = load 'mavenpipeline/pipeline.groovy'
        pipeline.helloworld()
        pipeline.addition()
        pipeline.build()
        pipeline.deployimage()
        
        
    }
